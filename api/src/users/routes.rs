// use axum::Router;
// use tower_http::trace::TraceLayer;
// use tower_http::cors::{Any, CorsLayer};
// use tower::ServiceBuilder;
// use crate::app;
//
//
// fn users_routes(router: &Router) -> &Router{
//     router.route("/", post(json))
//                       .layer(ServiceBuilder::new().layer(cors))
// }
